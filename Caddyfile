{$DOMAIN} {
    # API and downloads proxy to backend
    reverse_proxy /api/* backend:8000
    reverse_proxy /health backend:8000

    # Everything else goes to Next.js frontend
    reverse_proxy * frontend:3000

    # Upload limit — datasets can be large
    request_body {
        max_size 200MB
    }

    encode gzip
}
