# DO NOT commit real secrets — replace placeholder values and apply with kubectl.
# Create with: kubectl apply -f k8s/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: autods-secrets
  namespace: autods-ai
type: Opaque
stringData:
  GOOGLE_API_KEY: "REPLACE_ME"
  APP_ACCESS_PASSWORD: "REPLACE_ME"
